```{r}
library(zonator)

setup.dir <- system.file("extdata/tutorial/basic", package="zonator")
test.project <- create_zproject(setup.dir)
variant.4 <- get_variant(test.project, 4)

p1 <- plot(variant.4, monochrome=FALSE, group=FALSE, statistic="mean", 
           invert.x=TRUE, main="Esimerkki")
p1

```